import React from 'react';
export const metadata = {
    title: "signin page",
    description: "description for home paga",
  };
  

const Page = () => {
    return (
        <div>
            Hello from sign in
        </div>
    );
}

export default Page;
